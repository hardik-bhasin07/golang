package main

import "fmt"

func main() {
	x, y, z := "Hardik", 12, 12.23
	fmt.Printf("%v is the value and %T is the type\n", x, x)
	fmt.Printf("%v is the value and %T is the type\n", y, y)
	fmt.Printf("%v is the value and %T is the type\n", z, z)

}
